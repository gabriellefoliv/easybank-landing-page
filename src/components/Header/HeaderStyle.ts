import styled from "styled-components";

export const HeaderContainer = styled.div`
    display: flex;
    flex-direction: column;
    /* z-index: inherit; */
    /* overflow-x: hidden;
    overflow-y: hidden; */
    
`;